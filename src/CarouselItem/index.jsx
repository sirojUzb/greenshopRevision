const CarouselItem = (props) => {
  const { bg } = props;
  return (
    <div style={{ width: "100%", height: "400px", 
    background: bg
     }}>
      CarouselItem
    </div>
  );
};

export default CarouselItem;
